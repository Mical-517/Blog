---
title: 哈工大os
published: 2026-03-13T20:43:56+08:00
summary: "观看笔记"
cover:
  image: https://bucket-qjy.oss-cn-qingdao.aliyuncs.com/picture/202603132045971.png
tags: [os]
categories: 'os'
draft: false
lang: ''
---



# 1.现在电脑开机通电了

首先，os是一个软件，运行就要装在memory，如何载入的呢？计算机有一点固有的引导程序BIOS，就使用来引导os开始运行工作

![image-20260313210515294](https://bucket-qjy.oss-cn-qingdao.aliyuncs.com/picture/202603132105316.png)

## 第一阶段：开始运行bootsect.s

bootsect程序，（一开始cpu处于实时模式，只能寻址16位），首先将自身程序转移到另一个位置，空出当前位置（后面解释），然后将setup程序模块装载到内存，将system模块装载到0地址处，这就是为什么一开始bootsect就要腾出空间，防止覆盖原来的程序。

主要作用：加载setup程序模块。



## 第二阶段：运行setup模块（完成os启动前的设置）



setup模块初始化一些硬件参数比如内存大小，方便后续os初始化时分配内存合理，使用一条高级汇编指令（cpu变为保护模式，32位），然后转移指令开始执行system（位于0地址处）



## 第三阶段：执行system程序

system是有许多模块组合的，这时候，先执行什么就尤为重要。

先执行head.s文件，之后进入main函数进行os的初始化（初始化一切os可掌握的资源情况）**注意这里main函数永远不会退出，很好理解，我们使用pc一定是要断电之后，os才不工作**。



# 二. 关于接口

## 1.上层应用调用系统接口的概念理解

1. 通用抽象接口的含义

   就是用户可以通过某一个东西或者方法来忽略内部复杂的实现进而实现想要功能的载体，这个东西或者方法就是接口

2. 理解系统接口

   切入：用户命令行来使用计算机，其实命令行（比如bash）就是一个程序，当我们键入一个命令（命令就是一个可执行程序），然后这个bash程序**就会使用一个系统接口fork来创建一个子进程用来工这个调用程序使用**，但是正如接口含义，我们不需要知道接口是如何实现并且可以被这个调用程序使用，我们只需要通过他来实现这个调用另一个程序的功能罢了

   总结：



![image-20260316212647880](https://bucket-qjy.oss-cn-qingdao.aliyuncs.com/picture/202603162126912.png)



## 2.系统接口在操作系统内部的运作机制（实现）

首先提出问题从问题切入：

上层应用为什么只能使用系统接口来实现对系统内核的访问，或者说计算机资源的访问

```c++
//比如whoami命令，得到当前使用系统的用户信息，但是注意这个信息存储在操作系统内核段，所以使用这个命令的程序，如果已知到用户名存储在系统内核在内存中的地址，为什么不直接取出来，而是通过whoami来取呢？
```

![image-20260316215529082](https://bucket-qjy.oss-cn-qingdao.aliyuncs.com/picture/202603162155330.png)



引入内核态（段）与用户态（段）

我们已知程序访问内存都是一段一段访问的，所以在不同的内存段给他划分为不同的状态，**那怎么划分的状态**：记得前面head.s初始化gdt表，表里的每个像的地址（一开始不只有操作系统的地址吗），会将最后的两位来表示什么这段地址是什么状态的

流程：现在执行一条指令（执行程序不就是执行指令），然后在gdt表里可以看到目前这段程序（指令）是处于什么段，然后将这个状态放到currentPL（cpl）里面，然后当这个程序要访问其他内存段的时候），同样根据gdt来查询DPL（目标段的特权值），如果特权比较低，就不能访问，这就是为什么普通程序不能直接反问操作系统内核里的数据



那么，不能·直接访问，还有什么办法可以访问操作系统呢内核(换句话说普通程序如何进入内核)

